apiVersion: warptail.exceptionerror.io/v1
kind: WarpTailService
metadata:
  name: example-2
  namespace: default
spec:
  enabled: true
  routes:
  - type: http
    domain: test.test.com
    machine:
      address: w2g
      port: 8080